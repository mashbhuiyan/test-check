require('../../app/models/request_log');
const mongoose = require('mongoose');
var assert = require('assert');
const RequestLog = mongoose.model('RequestLog');

describe('RequestLog', () => {
    // TODO: check insertion RequestLog data using mongoose
});
